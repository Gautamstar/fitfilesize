# Production build settings. Public values only: everything prefixed VITE_
# ends up in the shipped bundle or HTML. Real env vars (Vercel's) override
# anything set here.

# Canonical origin, used by scripts/seo-pages.mjs for canonical URLs, og:url
# and sitemap.xml.
VITE_SITE_URL=https://fitfilesize.com
